# Repository Workflow

1. Keep the `main` branch stable and compilable.
2. Make structural changes on a short-lived branch.
3. Use semantic version tags such as `v1.0.0` for stable releases.
4. Package Overleaf-ready ZIP files as GitHub Release assets rather than storing duplicate ZIP files in the source tree.
5. Keep project-specific content in separate repositories created from this template repository.
