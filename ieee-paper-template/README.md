# IEEE Paper Starter

This lightweight starter assumes the `IEEEtran` class is installed in the TeX distribution.

```bash
latexmk -pdf main.tex
```

For an actual submission, always download and follow the current template and author instructions supplied by the target IEEE conference or journal.
