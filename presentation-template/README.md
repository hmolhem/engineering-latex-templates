# Technical Presentation Template

Compile with:

```bash
latexmk -pdf main.tex
```

Speaker notes are written with `\note{...}`. Change `\setbeameroption{hide notes}` to `show notes` when preparing a notes copy.
