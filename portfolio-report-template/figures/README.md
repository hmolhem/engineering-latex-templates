# Figures

Place only curated, publication-safe figures in this directory. Use stable filenames and
record the originating run or test ID in each caption. Prefer vector PDF for plots and
diagrams; use PNG for screenshots when vector export is unavailable.

A missing figure referenced through `\maybeincludegraphics` produces a visible diagnostic
box rather than silently disappearing.
